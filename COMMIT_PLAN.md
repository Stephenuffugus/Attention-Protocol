# Commit Plan — 2026-04-21 Afternoon Push
## Staged for review. No commits executed until you say go.

All of today's afternoon work sits unstaged in the working tree. Below is the logical sequence for committing it. Each commit is a single coherent unit. Every commit message is factual — no marketing. Co-authored credit on each per standard practice.

---

## Pre-flight check

```bash
git status
# Should show: ~20 new/modified files, working tree not committed.
npm test
# Confirm 754/754 green before committing.
```

---

## Commit 1 — Time-bomb fix in signer test

**Files:** `tests/attention-signer.test.js`

**Why:** The VC-integration roundtrip test was using a hard-coded `generated_at` date + default 24h JWT expiry. After clock passed 2026-04-21T12:00Z, the test began failing with `token_expired`. Fix: pass explicit `validUntilMs: 10y` to the roundtrip test so the golden-fixture-vs-default-expiry collision never recurs.

**Message:**
```
Fix time-bomb in VC-integration signer test

Pass explicit validUntilMs to toSignedJwt roundtrip test to decouple
from the golden-fixture's fixed generated_at + default 24h JWT expiry.
Test was passing yesterday, failing today because system clock passed
the fixture's implicit expiry.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

---

## Commit 2 — Demo fixture refresher + first refresh

**Files:** `scripts/refresh-demo-fixtures.js` (new — single-responsibility; second commit extends)

**Why:** `humanness-sample.json` shipped with a 24h JWT validity window. Would have rendered "Credential expired" mid-pitch if you demoed after the expiry. New script re-signs with fresh 24h window. Idempotent and scripted so this never becomes a pitch-day surprise.

**Message:**
```
Add demo-fixture refresher to prevent in-pitch JWT expiry

humanness-sample.json shipped with 24h validity; after the window
elapsed, prove-humanness.html displayed a graceful 'Credential expired'
verdict — fine for non-pitch moments, embarrassing during a buyer demo.

New scripts/refresh-demo-fixtures.js re-signs the fixture with a fresh
24h window using the env signing key. Playbook Phase 4 now runs it
before evidence-kit regen.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

---

## Commit 3 — Receipt-wide gated composite (core shipped feature)

**Files:**
- `src/sdk/receipt-composite.js` (new)
- `tests/receipt-composite.test.js` (new — 27 tests)
- `src/sdk/verifiable-credentials.js` (fromReceipt now auto-populates compositeScoreFinal + gatesApplied)
- `proof/run-bot-vs-human.js` (prints RECEIPT-WIDE COMPOSITE (GATED) block side-by-side with behavioral)
- `proof/verify.html` (Behavioral card renders both composites + gates list)

**Why:** Pre-existing behavioral composite alone produces a 0.004 gap vs LLM Paster bots. New gated composite caps the final receipt-wide score on independent integrity layers (environmental, composition, honeypot). Every bot in the harness gates to 0.300; human stays at 0.573 → gap 0.273. Receipt now self-describes WHY via gatesApplied provenance array.

**Message:**
```
Ship receipt-wide gated composite (defense-in-depth aggregation)

Pure behavioral composite has a 0.004-0.100 gap to well-engineered bots.
The environmental gate, composition integrity, and honeypot canary are
already collecting independent evidence — the gate caps the aggregate
final composite on those independent detectors rather than leaving the
behavioral score as the only summary number.

New src/sdk/receipt-composite.js:
- computeFinalComposite() applies min-cap from env / composition / honeypot
- 27 unit tests (tests/receipt-composite.test.js) covering all gate
  combinations, input defensiveness, and an Apr 21 regression fixture.

Integration:
- VC builder auto-populates credentialSubject.humanVerification.{
    compositeScoreFinal, qualityTierFinal, gatesApplied, compositeGateVersion
  } — existing compositeScore preserved for backward compat.
- Bot harness prints a side-by-side behavioral/gated comparison.
- verify.html Behavioral card now shows both numbers + the
  gatesApplied list when a gate triggered.

2026-04-21 harness result: 4/4 bots gate to 0.300 vs human baseline
0.573. Receipt-wide gap 0.273. Independently verifiable in every
fresh session going forward.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

---

## Commit 4 — 7-layer canonical fixture + verify.html card coverage

**Files:**
- `scripts/generate-7layer-fixture.js` (new)
- `proof/results/verify-sample-7layer.json` (new — generated by the script)
- `proof/verify.html` (adds render cards for 3b honeypot + 6b RFC 3161 TSA)
- `scripts/generate-evidence-kit.js` (default receipt pointer prefers 7-layer)
- `tests/canonical-fixtures.test.js` (new — 19 tests protecting all 4 shipped fixtures from silent drift)
- `dist/evidence-kit.zip` (if committed; else leave — it's dist/ anyway)

**Why:** Pre-existing fixtures had at most 4 layer fields in credentialSubject. The "seven layers" pitch line wasn't fully demonstrable. New fixture populates all 7. verify.html now renders all 8 cards when a 7-layer JWT is pasted. Evidence kit bundles the 7-layer fixture by default.

**Message:**
```
Add canonical 7-layer demo fixture + verify.html card coverage

Pre-existing fixtures populated at most 4 attestation layers in the
signed credentialSubject. Pitch line 'seven layers in one receipt' was
structurally demonstrable but no single fixture exercised the full stack.

New scripts/generate-7layer-fixture.js synthesizes a clean-human session
with every layer populated, runs through VC.fromReceipt (auto-includes
the gated composite), signs with the env key, and writes
proof/results/verify-sample-7layer.json. OTS proof_b64 and TSA token_b64
are placeholder strings — the fixture is structural, not a real anchored
session; docs explicitly note this.

verify.html additions:
- Layer 3b honeypot card (renders when cs.honeypotCanary present)
- Layer 6b RFC 3161 card (renders when cs.rfc3161Timestamp present)
- Card ordering now: 1 env -> 2 behavioral -> 3a composition ->
  3b honeypot -> 4 consent -> 5 ed25519 -> 6a btc -> 6b tsa

Evidence kit (scripts/generate-evidence-kit.js) prefers the 7-layer
fixture as its sample receipt.

Regression protection: new tests/canonical-fixtures.test.js asserts
per-fixture layer coverage (stephen-0573 = 0, humanness = 3,
verify-6layer = 4, verify-7layer = 7) so nobody silently downgrades.
All 19 tests pass; suite total: 754 green.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

---

## Commit 5 — Documentation push (the big one)

**Files:**
- `SEVEN_LAYER_DEEP_DIVE.md` (§0 rewrite + §2 signal count 15→20 + §2.5 new + §3/§4 citations corrected + §9 fixture table + §11 8 beats)
- `PRODUCT_VALIDATION_PLAYBOOK.md` (Phase 3 describes actual harness behavior; Phase 1.2 points to 7-layer fixture)
- `docs/cme-demo-script.md` (new — 90-sec narration + shot list)
- `docs/ai-honesty-cheat-card.md` (new — printable scripted answers)
- `docs/technical-faq.md` (new — 32 Q&As for QA teams)
- `docs/corpus-collection-kit.md` (new — invite copy + CLI tracker)
- `docs/prospect-dossiers/` (new directory — 20 per-company dossiers + README)

**Why:** Pre-pitch assets. Every doc stands on its own; committed together because they're a coherent outreach-prep bundle and the deep dive / playbook updates are tightly coupled to the gated composite + 7-layer fixture shipped above.

**Message:**
```
Add outreach prep docs + refresh deep dive and playbook

Deep dive rewrites:
- §0 frame distinguishes behavioral composite gap (0.004 worst case)
  from gated composite gap (0.273)
- §1 bot-profile count corrected 3 -> 4
- §2 signal count corrected 15 -> 20 (actual code in attention-protocol.js)
- §2.5 NEW section covers the receipt-wide gated composite with
  the 2026-04-21 measured data
- §3 citation (arxiv 2511.12468) verified; honest caveat added for
  paraphrase-attack F1 drop 97.2% -> 75.7%
- §4 Liu et al. citation corrected to 'Formalizing and Benchmarking...'
  (USENIX 2024, arxiv 2310.12815). Previously-claimed 90-95% canary
  detection figure was unsupported and is removed pending our own
  benchmark
- §9 fixture-coverage table added; points to 7-layer as the canonical
- §11 argument chain 7 -> 8 beats (adds gated composite)

Playbook:
- Phase 1.2 now points to verify-sample-7layer.json (full card render)
  instead of stephen-0573 (renders only 2 cards)
- Phase 3 describes actual 4-profile harness behavior + documents the
  honeypot gap (harness types fixed strings, doesn't exercise canary)
- Phase 4 runs scripts/refresh-demo-fixtures.js before evidence-kit regen

New docs:
- docs/cme-demo-script.md - 90-sec word-for-word narration, shot list,
  browser setup, anti-overclaim guardrails
- docs/ai-honesty-cheat-card.md - printable scripted answers to the
  four questions every buyer asks
- docs/technical-faq.md - 32 Q&As across 9 sections (architecture,
  privacy, Part 11, security, crypto, integration, scale, commercial,
  honest-gap list) for QA-team self-qualification
- docs/corpus-collection-kit.md + scripts/corpus-status.js - invite
  templates (SMS/email/Slack/LinkedIn), tagged URL, CLI tracker over
  firebase-admin
- docs/prospect-dossiers/ - 20 per-company dossiers (10 MECs,
  9 pharma compliance, 1 credentialing). Every named contact role
  flagged [VERIFY] for LinkedIn validation before sending.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

---

## Commit 6 — Signing-key rotation kit (staged, not applied)

**Files:**
- `scripts/rotate-signing-key.js` (new)
- `.gitignore` (adds `rotation-staging/`)

**NOT committed:** `rotation-staging/` itself — contains the new private key; stays gitignored.

**Why:** Security-posture work. The current SWS_SIGNING_KEY surfaced in session diagnostics. Rotation kit is staged (generates new kid + merged JWKS + cutover plan) but nothing live is applied. You execute the cutover when convenient via `rotation-staging/ROTATION_PLAN.md`.

**Message:**
```
Add signing-key rotation kit (staged; not applied)

Current SWS_SIGNING_KEY surfaced in session diagnostic output during
2026-04-21 troubleshooting. Precautionary rotation. No evidence of
external exposure, but best practice after any in-session leak.

scripts/rotate-signing-key.js generates:
- rotation-staging/new-jwks.json — merged JWKS with old + new kids
  so legacy receipts remain verifiable during a 7-day grace period
- rotation-staging/new-private-key.txt — new SWS_SIGNING_KEY
- rotation-staging/new-env-block.txt — ready to paste over .env
- rotation-staging/ROTATION_PLAN.md — step-by-step cutover

.gitignore now excludes rotation-staging/ so the new private key
never enters version control.

Nothing live is touched by running the script. Cutover is a separate
manual step documented in ROTATION_PLAN.md — deploy the new JWKS to
Firebase first, then update .env, then re-sign fixtures, then grace-
period-revoke the old kid.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

---

## Commit 7 — Morning plan refresh

**Files:** `MORNING_PLAN.md`

**Why:** Reflects today's end-state (754 tests, gated composite, 7-layer fixture, all docs shipped). Product-first ordering; YC pushed to the bottom per your instruction.

**Message:**
```
Refresh MORNING_PLAN.md for afternoon push end-state

Reflects 754/754 green, gated composite shipped, 7-layer fixture,
20 dossiers, FAQ, cheat card, CME script, corpus kit, rotation
staged. Product-first ordering: YC submission is step 11, after
all product work lands.

Open items captured: playbook browser phases (you), deep dive
study (you), CME video recording (you, ~4h), corpus invites (you,
15 min), LinkedIn pass on dossiers (you), 20 cold emails (you,
blocked on study), LLM harness (blocked on Anthropic support),
signing-key rotation cutover (staged), Bogazici empirical-priors
(blocked on dataset download).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

---

## Push

After all commits land clean:
```bash
git push origin main
```

---

## Rollback

If anything goes sideways after push, revert by commit in reverse order:
```bash
git revert <commit-hash>
```
No commit in this plan depends on the one before for correctness (tests pass independently), so reverts are safe.

---

**Ready when you are.** Say "commit them all" and I execute 1–7 sequentially, running `npm test` after each to confirm green, and stop on any failure. Or say "commit 1 through N" if you want a partial push. Or "push now" after reviewing if you want them all bundled into a single commit instead.
